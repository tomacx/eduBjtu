<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.StudentDao">

    <resultMap type=".entity.Student" id="StudentMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="studentnum" column="studentNum" jdbcType="VARCHAR"/>
        <result property="password" column="passWord" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="StudentMap">
        select
idnamestudentNumpassWord
        from student
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="StudentMap">
        select
idnamestudentNumpassWord
        from student
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="studentnum != null and studentnum != ''">
                and studentNum = #{studentnum}
            </if>
            <if test="password != null and password != ''">
                and passWord = #{password}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from student
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="studentnum != null and studentnum != ''">
                and studentNum = #{studentnum}
            </if>
            <if test="password != null and password != ''">
                and passWord = #{password}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into student(namestudentNumpassWord)
        values (#{name}#{studentnum}#{password})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into student(namestudentNumpassWord)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.name}#{entity.studentnum}#{entity.password})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into student(namestudentNumpassWord)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}#{entity.studentnum}#{entity.password})
        </foreach>
        on duplicate key update
name = values(name)studentNum = values(studentNum)passWord = values(passWord)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update student
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="studentnum != null and studentnum != ''">
                studentNum = #{studentnum},
            </if>
            <if test="password != null and password != ''">
                passWord = #{password},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from student where id = #{id}
    </delete>

</mapper>

