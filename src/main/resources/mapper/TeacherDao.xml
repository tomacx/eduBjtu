<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace=".dao.TeacherDao">

    <resultMap type=".entity.Teacher" id="TeacherMap">
        <result property="id" column="id" jdbcType="BIGINT"/>
        <result property="teachernum" column="teacherNum" jdbcType="VARCHAR"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TeacherMap">
        select
idteacherNumnamepassword
        from teacher
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TeacherMap">
        select
idteacherNumnamepassword
        from teacher
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="teachernum != null and teachernum != ''">
                and teacherNum = #{teachernum}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from teacher
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="teachernum != null and teachernum != ''">
                and teacherNum = #{teachernum}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into teacher(teacherNumnamepassword)
        values (#{teachernum}#{name}#{password})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into teacher(teacherNumnamepassword)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.teachernum}#{entity.name}#{entity.password})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into teacher(teacherNumnamepassword)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.teachernum}#{entity.name}#{entity.password})
        </foreach>
        on duplicate key update
teacherNum = values(teacherNum)name = values(name)password = values(password)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update teacher
        <set>
            <if test="teachernum != null and teachernum != ''">
                teacherNum = #{teachernum},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from teacher where id = #{id}
    </delete>

</mapper>

